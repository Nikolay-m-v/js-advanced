"use strict";

function cityRecord(name, population, treasury) {
  let record = { name: name, population: population, treasury: treasury };

  console.log(record);
  return record;
}

cityRecord("Santo Domingo", 12000, 23500);
